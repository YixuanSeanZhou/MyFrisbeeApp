//
//  CreateService.swift
//  MyFrisbeeApp
//
//  Created by Thomas on 2017/8/2.
//  Copyright © 2017年 Thomas. All rights reserved.
//

import Foundation
import UIKit
import FirebaseStorage
import FirebaseDatabase
/*
struct PostService {
    
    static func create(for image: UIImage, gameTitle: String, gameTime: String, gameLocation: String) {
        let imageRef = Storage.storage().reference().child("test_image.jpg")
            StorageService.uploadImage(image, at: imageRef) { (downloadURL) in
            guard let downloadURL = downloadURL else {
                return
            }
            
            PickupTableViewController.url = downloadURL.absoluteString
            
            create(forURLString: PickupTableViewController.url, aspectHeight: PickupTableViewController.aspectHeight, gameTitle: gameTitle, gameTime: gameTime, gameLocation: gameLocation)
        }
    }
    

    
}


*/
